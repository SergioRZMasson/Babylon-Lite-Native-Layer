{
  "name": "babylon-lite-native",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TypeScript Babylon-Lite API mirror whose implementation forwards to the native C++/bgfx engine, plus the scene entry points bundled (one self-contained JS per scene) for the native host.",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "typescript": "^5.7.0"
  }
}
