# Build output
/build/
/build-min/
/build-poly/
/build-v8/

# TypeScript bundler: dependencies + generated bundles (produced by `node js/build.mjs`,
# copied to <build>/bin/js by the CMake js_bundles target).
js/node_modules/
js/dist/

# Scratch verification artifacts
*.tga
*.png

# Large downloaded model assets
assets/*.glb
assets/Alien.gltf
assets/Alien.bin

# Tooling deps (esbuild for tools/translate_scenes.mjs) + parity/bench run output
node_modules/
package-lock.json
/build/parity-out/
/tools/bench/out/
