# Build output
/build/
/build-min/

# Scratch verification artifacts
*.tga
*.png

# Large downloaded model assets
assets/*.glb

# Tooling deps (esbuild for tools/translate_scenes.mjs) + parity run output
node_modules/
package-lock.json
/build/parity-out/